export type CheckInType = {
  account: string;
  dateTime: string;
  diagnosId: string;
  code: number;
};

export type OptionType = {
  value: number;
  label: string;
};

export type DiagnosType = {
  id: number;
  diagnosName: string;
};
